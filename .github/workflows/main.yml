    # ... (các bước Setup Python và PlatformIO)

    # ------------------------------------------------------------------
    # Tải và sửa lỗi thư viện ESP32-AudioI2S v2.0.4 (ỔN ĐỊNH NHẤT)
    # ------------------------------------------------------------------
    - name: 🎧 Download & Fix ESP32-AudioI2S Library
      run: |
        TAG_VERSION="2.0.4"  # <--- KHUYẾN NGHỊ DÙNG 2.0.4 ĐỂ TRÁNH LỖI I2S_STD.H
        
        # Đường dẫn tải xuống và thư mục
        ZIP_URL="https://github.com/schreibfaul1/ESP32-audioI2S/archive/refs/tags/${TAG_VERSION}.zip"
        LIB_DIR="lib/ESP32-audioI2S-${TAG_VERSION}"
        AUDIO_HEADER="${LIB_DIR}/src/Audio.h"
        
        # Tải, giải nén
        mkdir -p lib
        curl -L -o lib/audio.zip ${ZIP_URL}
        unzip -o lib/audio.zip -d lib/
        
        echo "🛠️ Sửa lỗi NetworkClient.h và các khai báo class..."
        
        # Sửa #include <NetworkClient.h> -> #include <WiFiClient.h>
        perl -pi -e 's/#include <NetworkClient.h>/#include <WiFiClient.h>/g' "${AUDIO_HEADER}"
        
        # Sửa khai báo biến: NetworkClient -> WiFiClient (Khắc phục lỗi sâu)
        perl -pi -e 's/NetworkClient(\*?) client/WiFiClient\1 client/g' "${AUDIO_HEADER}"
        
        rm lib/audio.zip
