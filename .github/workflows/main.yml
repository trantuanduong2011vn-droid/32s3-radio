name: PlatformIO Build for LOLIN S3 Mini

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]
  # Cho phép chạy thủ công qua giao diện GitHub
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Code 🛎️
      uses: actions/checkout@v4
      with:
        submodules: true

    - name: Set up Python and PlatformIO 🛠️
      uses: actions/setup-python@v5
      with:
        python-version: '3.x'
      
    - name: Install PlatformIO Core
      run: pip install platformio

    # ------------------------------------------------------------------
    # BƯỚC KHẮC PHỤC LỖI THƯ VIỆN AUDIOI2S (CHẠY TRÊN THƯ MỤC GỐC MASTER)
    # Tải thư viện, giải nén và sửa lỗi NetworkClient.h
    # ------------------------------------------------------------------
    - name: Tải, Giải nén và Sửa lỗi Thư viện Audio I2S 🎧
      run: |
        # Định nghĩa các biến
        GITHUB_REPO_NAME="ESP32-audioI2S"
        REPO_BRANCH="master" 
        
        # Tên thư mục sau khi giải nén: ESP32-audioI2S-master
        DIR_NAME_AFTER_UNZIP="${GITHUB_REPO_NAME}-${REPO_BRANCH}"
        
        # URL tải file ZIP của branch 'master'
        DOWNLOAD_URL="https://github.com/schreibfaul1/ESP32-audioI2S/archive/refs/heads/${REPO_BRANCH}.zip"
        
        # Tên file ZIP tạm thời
        TEMP_ZIP_NAME="lib/audio_temp_master.zip" 
        
        # 1. Tạo thư mục lib và tải file ZIP
        mkdir -p lib
        echo "Đang tải thư viện từ branch ${REPO_BRANCH}..."
        curl -L ${DOWNLOAD_URL} -o ${TEMP_ZIP_NAME}
        
        # 2. Giải nén file TẠM THỜI vào thư mục lib/
        unzip -o ${TEMP_ZIP_NAME} -d lib/
        
        # 3. Sửa lỗi Biên dịch NetworkClient.h (BẮT BUỘC)
        # Sửa file Audio.h nằm ở lib/ESP32-audioI2S-master/src/Audio.h
        FILE_TO_FIX="lib/${DIR_NAME_AFTER_UNZIP}/src/Audio.h"
        echo "Đang sửa lỗi NetworkClient.h trong ${FILE_TO_FIX}"
        
        # Sử dụng sed để thay thế: #include <NetworkClient.h> -> #include <WiFiClient.h>
        sed -i 's/#include <NetworkClient.h>/#include <WiFiClient.h>/g' "${FILE_TO_FIX}"
        
        # 4. Xóa file zip tạm thời
        rm ${TEMP_ZIP_NAME}
        echo "Đã hoàn tất cấu hình thư viện Audio."

    # ------------------------------------------------------------------
    # BƯỚC BIÊN DỊCH CUỐI CÙNG
    # ------------------------------------------------------------------
    - name: Run PlatformIO Build Command 🚀
      run: |
        # Lệnh này sẽ tự động cài đặt các thư viện còn lại từ platformio.ini 
        # và sử dụng thư viện Audio đã được sửa lỗi trong lib/
        pio run
